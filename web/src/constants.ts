import type { FilterSettings } from "@/types/index";

export const DARKO_MODE_ID = "darko-mode-style";

export const DEFAULT_FILTERS: FilterSettings = {
  brightness: 100,
  contrast: 100,
  saturation: 100,
  sepia: 0,
  grayscale: 0,
};
